declare class NoiseGeneratorWorklet extends AudioWorkletProcessor {
    static get parameterDescriptors(): {
        name: string;
        defaultValue: number;
        minValue: number;
        maxValue: number;
    }[];
    process(inputs: any, outputs: any, parameters: Record<string, Float32Array>): boolean;
}
