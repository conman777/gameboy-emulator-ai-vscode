import GameBoy from "../GameBoy";
export default class AudioChannel {
    protected gameboy: GameBoy;
    enabled: boolean;
    leftChannelEnabled: boolean;
    rightChannelEnabled: boolean;
    canPlay: boolean;
    constructor(gameboy: GameBoy);
}
