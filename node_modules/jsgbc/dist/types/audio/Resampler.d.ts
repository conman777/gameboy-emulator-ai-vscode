export default class Resampler {
    private fromSampleRate;
    private toSampleRate;
    private channels;
    private outputBufferSize;
    ratioWeight: number;
    lastWeight: number;
    tailExists: boolean;
    outputBuffer: Float32Array;
    lastOutput: Float32Array;
    constructor(fromSampleRate: number, toSampleRate: number, channels: number, outputBufferSize: number);
    resample(buffer: Float32Array): number;
}
